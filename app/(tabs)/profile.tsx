import { useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, TextInput, useWindowDimensions, View } from "react-native";
export default function ProfileScreen() { const [fullName, setFullName] = useState(""); const [email, setEmail] = useState(""); const [error, setError] = useState(""); const [success, setSuccess] = useState(""); const { width } = useWindowDimensions(); const saveProfile = () => { const validEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim()); if (!fullName.trim()) { setSuccess(""); setError("Please enter your full name."); return; } if (!validEmail) { setSuccess(""); setError("Enter a valid email address, such as you@example.com."); return; } setError(""); setSuccess("Profile saved! Your EventMate details are up to date."); }; const clearFeedback = () => { setError(""); setSuccess(""); }; return <ScrollView contentContainerStyle={[styles.container, width >= 700 && styles.wideContainer]} keyboardShouldPersistTaps="handled"><Text style={styles.title}>My profile</Text><Text style={styles.subtitle}>Keep your campus event details current.</Text><View style={styles.form}><Text style={styles.label}>Full name</Text><TextInput accessibilityLabel="Full name" value={fullName} onChangeText={(value) => { setFullName(value); clearFeedback(); }} placeholder="Enter your full name" style={styles.input} /><Text style={styles.label}>Email address</Text><TextInput accessibilityLabel="Email address" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={(value) => { setEmail(value); clearFeedback(); }} placeholder="you@example.com" style={styles.input} />{!!error && <Text accessibilityRole="alert" style={styles.error}>{error}</Text>}{!!success && <Text accessibilityRole="alert" style={styles.success}>{success}</Text>}<Pressable onPress={saveProfile} style={styles.button}><Text style={styles.buttonText}>Save profile</Text></Pressable></View></ScrollView>; }
const styles = StyleSheet.create({ button: { alignItems: "center", backgroundColor: "#2563EB", borderRadius: 12, marginTop: 12, padding: 16 }, buttonText: { color: "#FFFFFF", fontSize: 16, fontWeight: "800" }, container: { backgroundColor: "#F7F9FC", flexGrow: 1, padding: 24, paddingTop: 64 }, error: { color: "#B42318", fontSize: 14, fontWeight: "600", marginBottom: 4 }, form: { backgroundColor: "#FFFFFF", borderColor: "#DFE6F1", borderRadius: 18, borderWidth: 1, padding: 20 }, input: { borderColor: "#C7D1E0", borderRadius: 10, borderWidth: 1, color: "#172033", fontSize: 16, marginBottom: 20, padding: 14 }, label: { color: "#29364A", fontSize: 15, fontWeight: "800", marginBottom: 8 }, subtitle: { color: "#5E6B7E", fontSize: 16, marginBottom: 24 }, success: { color: "#137333", fontSize: 14, fontWeight: "700", marginBottom: 4 }, title: { color: "#172033", fontSize: 30, fontWeight: "800" }, wideContainer: { alignSelf: "center", maxWidth: 700, width: "100%" } });
